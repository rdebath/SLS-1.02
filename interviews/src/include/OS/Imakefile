XCOMM
XCOMM include files for using classes that encapsulate OS-dependent functions
XCOMM

HEADERS = *.h
DEST = $(INCDIR)/OS

InstallMultipleFlags($(HEADERS),$(DEST),$(INSTINCFLAGS))

gendefs:
	./_gendefs
