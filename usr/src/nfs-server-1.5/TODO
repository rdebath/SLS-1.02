TODO:

- Ugid daemon?
- Better documentation
- re-read of /etc/exports on receipt of SIGHUP
- discriminate mount point permissions on a per NFS request basis
- maintain rmtab of clients with remote mounts
- test NFS over TCP
- add optional fsync on writes
- change export file syntax to a more common format
- keep and report performance statistics
- add support for invisible directory heirarchies, i.e. inverse exporting
