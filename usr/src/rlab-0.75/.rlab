//
// .rlab
//

// Caculate pi

pi = atan(1.0)*4.0;

// Calculate machine epsilon

epsilon = function() 
{
  eps = 1.0;
  while((eps + 1.0) != 1.0) 
  {
    eps = eps/2.0;
  }
  return eps;
};

epsilon();
