mke2fs
======
	- change the blocks par group ratio if the last group is too small
	  to contain super block + descriptors + blocks bitmap + inodes
	  bitmap + inode table + a few data blocks

e2fsck
======
	- better correction of bad directory entries
