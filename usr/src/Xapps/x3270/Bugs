Bugs in the original 3270tool code:

key_FTab skips a field when positioned on an FA before the field.  (It moves
to the second field instead)

get_field_attriubte wraps past 0.  (I'm not sure if this is a bug, but
changing it fixed one problem, and caused no new ones.)
