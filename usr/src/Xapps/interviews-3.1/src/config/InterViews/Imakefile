XCOMM
XCOMM config files for building InterViews libraries and applications
XCOMM

CFFILES = template *.def *.cf
DEST = $(CONFIGDIR)/InterViews

InstallMultipleFlags($(CFFILES),$(DEST),$(INSTDATFLAGS))
