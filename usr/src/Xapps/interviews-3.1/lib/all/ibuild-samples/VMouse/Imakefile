XCOMM
XCOMM Subclass and unidraw file for VMouse
XCOMM

FILES = VMouse.c VMouse.h vmouse-main.c vmouse
DEST = $(LIBALLDIR)/ibuild-samples/VMouse

InstallMultipleFlags($(FILES),$(DEST),$(INSTDATFLAGS))
