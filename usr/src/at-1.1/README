This is version 1.1 of the at/atrun pair for running commands on a regular
basis. To install, do a

make .depend
make install

as root.

Do not forget to put

* * * * * /usr/lib/atrun

into root's crontab file and to start up crond in /etc/rc.

Bug reports to:
	ig25@rz.uni-karlsruhe.de

If at all possible, I'd appreciate you telling me which version you found
any bug in; to find it, use either ident(1) on the executable if you've got
RCS installed, or the strings command followed by a grep "\$Id".
