This directory contains sources for an MGR plot(5) filter.  I should
see if I can get a plot(5) manual page as well.  plot(5) is rarely
used UNIX standard for vector grafics exchange, so I don't know if it
is worth the effort.

You can test this program with:

   mgrplot < plot.ex

Have fun

Michael
