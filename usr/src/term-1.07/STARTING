Getting term running. The quick start guide...

Create a directory "~/.term" at both ends. I.e. do "cd $HOME" and then
"mkdir .term". The create a file called termrc in that directory with
the following lines...

# put the following two lines into ~/.term/termrc
escape 0-31
escape 128-159

