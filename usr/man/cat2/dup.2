


DUP(2)                                                     DUP(2)


NNAAMMEE
       dup, dup2  - duplicate fildescriptors

SSYYNNOOPPSSIISS
       ##iinncclluuddee<<uunniissttdd..hh>>
       iinntt dduupp ((iinntt ffiillddeess));;
       iinntt dduupp22 ((iinntt oolldd,, iinntt nneeww));;

DDEESSCCRRIIPPTTIIOONN
       dduupp(())  duplicates  the  filedescriptor _f_i_l_e_d_e_s and returns
       either the duplicated file descriptor.

       dduupp22(()) duplicates the file descriptor _o_l_d_, and  returns  a
       new file descriptor with a value equal to _n_e_w_f_d_.  If _n_e_w_f_d
       was open, it is closed before duplication.

       dduupp(()) and dduupp22(()) both return negative values on error.

EERRRROORRSS
       --EEBBAADDFF is returned when an bad file descriptor  is  passed
       to  either, for the original file descriptor that is being
       duplicated.

       --EEIINNVVAALL  is  returned   by  dduupp22(())  when  an  invalid  _n_e_w
       descriptor is passed.

       --EEMMFFIILLEE  is  returned  when  the  system  runs out of file
       descriptors. See ooppeenn((22)) for a discussion on how  to  cir-
       cumvent this.

FFIILLEESS
       linux/fs/fcntl.c,                /usr/include/linux/sys.h,
       /usr/include/unistd.h

SSEEEE AALLSSOO
       open(2)





















                          March 28, 1992                        1


