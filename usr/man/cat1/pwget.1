


PWGET(UTIL)                                           PWGET(UTIL)


NNAAMMEE
       pwget, grget - get password and group information

SSYYNNOOPPSSIISS
       ppwwggeett [ --nn name | --uu uid ]]

       ggrrggeett [ --nn name | --gg gid ]]

DDEESSCCRRIIPPTTIIOONN
       _P_w_g_e_t  and  _g_r_g_e_t are used to access the information found
       in //eettcc//ppaasssswwdd and //eettcc//ggrroouupp.  These routines  provide  a
       common access method whether using the Yellow Page network
       database or not.  The output of these routines is sent  to
       standard  output.  With no options, _p_w_g_e_t and _g_r_g_e_t output
       all of  the  data  found  using  _g_e_t_p_w_e_n_t_(LIBC_)  and  _g_e_t_-
       _g_r_e_n_t_(LIBC_)  respectively.   When  options are given, only
       specified entries are searched for.

       The options for _p_w_g_e_t are:

              --nn _n_a_m_e Output the first entry that  matches  using
                      ggeettppwwnnaamm((_n_a_m_e))..

              --uu _u_i_d  Output  the  first entry that matches using
                      ggeettppwwuuiidd((_u_i_d))..

       The options for _g_r_g_e_t are:

              --nn _n_a_m_e Output the first entry that  matches  using
                      ggeettggrrnnaamm((_n_a_m_e))..

              --gg _g_i_d  Output  the  first entry that matches using
                      ggeettggrrggiidd((_g_i_d))..

RREETTUURRNN VVAALLUUEE
       These routines return 0 upon success, 1  when  a  specific
       search fails and 2 upon error.

WWAARRNNIINNGGSS
       If  the  Yellow Page network database is in use and the YP
       client daemon, _y_p_b_i_n_d(ADMIN), is not  connected  to  a  YP
       server  daemon,  _y_p_s_e_r_v(ADMIN),  then these utilities will
       wait until such a connection is established.   These  rou-
       tines  can  be  terminated  in this condition by sending a
       SIGINT signal to the process (see _k_i_l_l(UTIL)).

AAUUTTHHOORR
       Pwget and grget were developed by Hewlett-Packard Company.

FFIILLEESS
       l     l.     /etc/group     Local    group    data    file
       /etc/passwd    Local password data file





                                                                1





PWGET(UTIL)                                           PWGET(UTIL)


SSEEEE AALLSSOO
       getgrent(LIBC), getpwent(LIBC), group(FILE), passwd(FILE).























































                                                                2


