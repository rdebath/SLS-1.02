# Sample of "minicom.users" (should be in /usr/local/etc)
# Format :  User name line [line..]
# Minicom saves it's setup information in /usr/local/etc/minirc.dfl.
# Dfl is default. So normally, everyone should have a default entry.
# 
#
# Who may use minicom?
#
# User		line	[..line]
#
root		dfl	modem
user1		dfl	modem
falc		dfl	modem
techs		dfl	modem
dent		dfl	modem

